function productRecipe(productId){return recipes.filter(r=>r.product_id===productId);}function stockByName(name){return ingredients.find(i=>i.name.toLowerCase()===name.toLowerCase());}
function renderProducts(){productsGrid.innerHTML=products.map(p=>`<div class="panel pizza-card"><div class="pizza-img"></div><div class="pizza-title">${p.name}</div><div class="price">${fmt(p.price)}</div><button class="primary" onclick="addToCart('${p.id}')">Ajouter</button></div>`).join("")||`<div class="panel">Aucune pizza créée.</div>`;}
function addToCart(productId){const p=products.find(x=>x.id===productId);const found=cart.find(x=>x.id===productId);if(found)found.qty+=1;else cart.push({id:p.id,name:p.name,price:Number(p.price),qty:1});renderCart();}
function clearCart(){cart=[];renderCart();}
function renderCart(){cartBox.innerHTML=cart.length?cart.map(i=>`<div class="cart-line"><span>${i.qty} × ${i.name}</span><strong>${fmt(i.qty*i.price)}</strong></div>`).join(""):`<p>Aucune pizza dans le panier.</p>`;const total=cart.reduce((sum,i)=>sum+i.price*i.qty,0);const {ht,tax}=taxFromTTC(total);cartTotal.textContent=fmt(total);cartTax.textContent=fmt(tax);cartHT.textContent=fmt(ht);}
function neededStock(){const needed={};for(const item of cart){for(const r of productRecipe(item.id)){needed[r.ingredient_name]=(needed[r.ingredient_name]||0)+Number(r.quantity)*item.qty;}}return needed;}
async function checkout(){if(!cart.length){alert("Panier vide.");return;}const needed=neededStock();for(const [name,qty] of Object.entries(needed)){const ingredient=stockByName(name);if(!ingredient){alert("Ingrédient manquant dans le stock : "+name);return;}if(Number(ingredient.stock)<qty){alert(`Stock insuffisant : ${name}`);return;}}for(const [name,qty] of Object.entries(needed)){const ingredient=stockByName(name);const {error}=await db.from("ingredients").update({stock:Number(ingredient.stock)-qty}).eq("id",ingredient.id);if(error){alert(error.message);return;}}const rows=cart.map(i=>({product_name:i.name,quantity:i.qty,total:i.price*i.qty,cashier:currentStaff.discord_name}));const {error}=await db.from("sales").insert(rows);if(error){alert(error.message);return;}cart=[];await loadAll();alert("Vente enregistrée.");}
